#include <stdio.h>

int main(){
   
    int i =0;       //innitialisation
    while (i<5) {      // will print until 4       * Condition
        printf("%d\n", i);  // code to run
        i++;           // iteration
    }
    return 0; 
}